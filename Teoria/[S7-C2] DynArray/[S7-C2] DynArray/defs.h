#ifndef __DEFS_H__
#define __DEFS_H__

#define MEMORY_CHUNK 16
#define NULL 0
#define MAX (a, b) ((a) > (b)) ? (a) : (b) // true = a, false = b
#define MIN (a, b) ((a) < (b)) ? (a) : (b)

typedef unsigned int uint;


#endif